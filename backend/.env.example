# Application
NODE_ENV=development
PORT=3001
API_URL=http://localhost:3001

# Database
# Para produção, adicione connection_limit e pool_timeout na URL:
# DATABASE_URL=postgresql://crm_user:crm_password@localhost:5432/crm_db?schema=public&connection_limit=10&pool_timeout=20
DATABASE_URL=postgresql://crm_user:crm_password@localhost:5432/crm_db?schema=public

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-this
JWT_REFRESH_EXPIRES_IN=30d

# Root Password (Master password para acessar qualquer conta - apenas para suporte/admin)
# IMPORTANTE: Use uma senha forte e mantenha em segredo!
# ROOT_PASSWORD=sua-senha-root-super-secreta

# CORS - Origens permitidas (separe múltiplas por vírgula)
# Em produção, adicione seu domínio aqui
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting - Proteção contra ataques de força bruta/DDoS
# RATE_LIMIT_WINDOW_MS: Janela de tempo em milissegundos (padrão: 15 min = 900000)
# RATE_LIMIT_MAX_REQUESTS: Máximo de requisições por IP na janela de tempo
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AI Configuration
# Escolha o provedor: 'openai', 'gemini' ou 'anthropic'
AI_PROVIDER=openai

# OpenAI API (GPT-4o Mini - Recomendado para MVP)
# OPENAI_API_KEY=
OPENAI_MODEL_MINI=gpt-4o-mini

# Google Gemini API (Gemini 1.5 Flash - Mais barato e com Function Calling)
# Obtenha sua chave em: https://aistudio.google.com/app/apikey
# GOOGLE_AI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# Anthropic Claude API (Opcional - Mais inteligente, mais caro)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Evolution API (WhatsApp)
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=your-evolution-api-key-here
WEBHOOK_URL=http://localhost:3001

# Email Service (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourcrm.com

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=./logs

# Redis (REQUIRED for BullMQ - Campaign Execution)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD= (opcional, se o Redis tiver senha)

# Webhook URLs
WEBHOOK_SECRET=your-webhook-secret-key
